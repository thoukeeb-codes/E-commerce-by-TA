import { Box, Heading } from "@chakra-ui/react";

function Product(props) {
  console.log("props", props);
  return (
    <Heading textAlign="center" p={200}>
      work in progress
    </Heading>
  );
}
export default Product;
